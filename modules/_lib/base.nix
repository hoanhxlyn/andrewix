# Base settings shared by all hosts: UI, profiles (users), default browser.
{
  terminal = {
    fontSize = 12;
    padding = 2;
    opacity = 0.8;
    name = "alacritty";
  };
  defaultBrowser = "helium";
  users.andrew = {};
}
